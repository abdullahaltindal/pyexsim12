* Misc object cannot be shared for multiple simulations.

* Add fs to filtering function in get_acc, so that user input will directly be in frequencies,  not btwn 0-1

* Prevent attempting to run the simulation if the input file is not saved.

* save_acc

* Change integration algorithm